{
  "name": "runbookcase",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Walmart freight tender → SHV TMS load bridge: fetch, sanitize per SOP, push to the SOR",
  "scripts": {
    "dev": "node scripts/dev-server.js",
    "test": "node --test"
  }
}
